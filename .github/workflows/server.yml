name: "Upload SARIF"

# Run workflow each time code is pushed to your repository.
on:
  push: 

jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2.3.4
    - name: Run SmartBugs Static Analysis
      uses: dindonero/smartbugs-local-static-analysis@b1.1
      with:
        tool: 'conkas,oyente'
