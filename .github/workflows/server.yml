name: "Upload SARIF"

# Run workflow each time code is pushed to your repository.
on:
  push: 

jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
    - name: Run SmartBugs Static Analysis
      uses: dindonero/smartbugs-local-static-analysis@b1.2
      with:
        tool: 'conkas oyente'
